{
  "comment-1": "create in terminal this json file (npm init -y) -y is used so the details are not required from user",
  "comment2": "install dependencies like mongoose express and handle bars(this is a template engine)-(npm i express,npm i mongoose,npm i hbs)",
  "comment3": "create source folder--(mkdir src), then create database folder in src to use mongodb connection--(mkdir models)",
  "comment4": "create an empty App.js in src using(echo > App.js)",
  "comment 5": "in script tag in package.json using nodemon(start:nodemon src/app.js) nodemon - helps in development without calling node everytime ",
  "name": "crypaybackend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "comment":"start script is to run the nodemon and (-e) is given so that .html .js .css files also reloads node when saved",
    "start": "nodemon src/app.js -e html,js,css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "hbs": "^4.2.0",
    "mongoose": "^7.0.2",
    "nodemon": "^2.0.22"
  }
}